name: Auto Update Animated Profile

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"
  push:
    branches:
      - main
      - master
    paths:
      - "profile_config.json"
      - "scripts/generate_animated_profile.py"
      - ".github/workflows/update-animated-profile.yml"
      - "README.md"

permissions:
  contents: write

jobs:
  update-profile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Generate animated profile SVG
        run: python scripts/generate_animated_profile.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PROFILE_USERNAME: ${{ github.repository_owner }}
          SOCIAL_INSTAGRAM: ${{ vars.SOCIAL_INSTAGRAM }}
          SOCIAL_LINKEDIN: ${{ vars.SOCIAL_LINKEDIN }}
          SOCIAL_EMAIL: ${{ vars.SOCIAL_EMAIL }}

      - name: Commit generated profile UI
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/animated-profile.svg README.md profile_config.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore: auto update animated profile"
            git push
          fi
